// Precisamos pensar no caso da variavel nao existir, por isso usamos uma condicao de else para tratar o erro
// Antes estava apenas itensTableView?.reloadData()

// Delegate é um design pattern,
// Reaproveitacao - no java é interface
// Quando usamos protocolo nao precisamos usar o override porque nao estamos trocando a implementacao do metodo
